# 贡献指南

提交问题时，请提供复现步骤、预期结果、实际结果和测试环境。使用合成数据，先检查附件中是否有账号、凭据、设备标识或私人画面。

修改代码时：

- 一次解决一个问题，在负责该行为的接口上补充回归。
- 按 [README](README.md#开发) 运行 Python、Node 测试和文件检查。
- 区分离线测试、本地启动和真机验证，注明未验证的部分。
- 保持 Stop/Continue、Suite 身份、设备绑定和证据归属不变；执行相关改动先读 [架构](docs/architecture.md)。
- 特定小程序的流程放在 `examples/`，不要写进通用设备工具。
- 新增公开文件时更新 `public-files.json`，不要包含本地配置或运行数据。

提交贡献前请确认有权提供这些代码。除另有约定外，贡献按项目的 [Apache-2.0 许可证](LICENSE) 接收。
